# Pinned loosely: pytest-homeassistant-custom-component pulls the matching
# Home Assistant release, so bumping it is how this tracks core.
pytest-homeassistant-custom-component>=0.13.316
# The manifest depends on `frontend`, which Home Assistant cannot set up
# without this wheel - and phacc deliberately leaves it out.
home-assistant-frontend
gtfs-realtime-bindings>=2.2.0
pytest-cov>=7.1.0
# Exact, and read by the Ruff CI job via version-file, so a lint run here and
# a lint run in CI are the same lint run. Dependabot moves it.
ruff==0.16.3
